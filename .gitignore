node_modules/
dist/
*.log
.DS_Store
.env
.env.local
*.tsbuildinfo

# App-managed runtime data (db, knowledge, skills, logs, worktrees, token)
data/
# Pre-existing import snapshots — imports now write these under data/backups/
data-backup-*/

# IDE
.idea/

# Accidental tsc emit (shared is consumed as .ts source)
shared/src/*.js
